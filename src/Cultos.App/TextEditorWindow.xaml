<Window x:Class="Cultos.App.TextEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar contenido" Width="620" Height="520" MinWidth="500" MinHeight="330"
        WindowStartupLocation="CenterOwner" Background="{StaticResource AppBackground}"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeadingText" Text="Editar contenido" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,18"/>
        <TextBlock x:Name="TitleLabel" Grid.Row="1" Text="Título" Foreground="{StaticResource Muted}" Margin="0,0,0,6"/>
        <TextBox x:Name="TitleBox" Grid.Row="2" Margin="0,0,0,14"/>
        <TextBlock x:Name="ContentLabel" Grid.Row="3" Text="Contenido" Foreground="{StaticResource Muted}" Margin="0,0,0,6"/>
        <TextBox x:Name="ContentBox" Grid.Row="4" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Click="Cancel_Click" Margin="0,0,8,0">Cancelar</Button>
            <Button Click="Save_Click" Style="{StaticResource PrimaryButton}">Guardar</Button>
        </StackPanel>
    </Grid>
</Window>